List memory = ['Monastir', 'Tunis', 'Paris' , 'Canada', 'New York'];

class ValueData {
  dynamic temp;
  dynamic wind;
  String weatherIcon;
  ValueData({this.temp, this.wind, required this.weatherIcon});
}